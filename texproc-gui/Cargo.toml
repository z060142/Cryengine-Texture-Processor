[package]
name = "texproc-gui"
version = "2.0.1"
edition = "2021"

[dependencies]
converter = { path = "../converter" }
eframe = { version = "0.33", default-features = false, features = ["default_fonts", "glow"] }
serde_json = "1.0"
texproc = { path = "../texproc" }
